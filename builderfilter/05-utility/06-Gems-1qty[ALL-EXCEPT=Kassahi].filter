// Gem stack quantity (split out of 06-Gems so the Kassahi family, which adds the
// quantity inside its gem frames in 02-Kassahi_gems*, doesn't show it twice)
// Formatting for stacked vs unstacked with warning
ItemDisplay[GEM>0 QTY>1 QTY<51]: %NAME% %QTY_TAG%%CONTINUE%{%NAME%}
